# =============================================================================
# Stylist MCP Server Configuration
# Copy this file to .env and customize values
# =============================================================================

# Data Paths
# Path to DressCode dataset root directory
DRESSCODE_ROOT=/path/to/data/DressCode

# Path to ChromaDB persistence directory
CHROMADB_PATH=/path/to/chroma_db

# =============================================================================
# LLM Configuration
# =============================================================================

# LLM Provider: 'anthropic' (Agent Maestro), 'azure_openai', or 'openai'
# - anthropic: Use Agent Maestro or direct Anthropic API (development)
# - azure_openai: Use Azure OpenAI (production recommended)
# - openai: Use direct OpenAI API
LLM_PROVIDER=anthropic

# --- Anthropic / Agent Maestro Configuration ---
LLM_API_ENDPOINT=http://localhost:23333/api/anthropic/v1/messages
MODEL_NAME=claude-3-5-haiku-20241022
# ANTHROPIC_API_KEY=sk-ant-xxx  # Only needed for direct Anthropic API

# --- Azure OpenAI Configuration (Production) ---
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
# AZURE_OPENAI_API_KEY=your-api-key
# AZURE_OPENAI_DEPLOYMENT=gpt-4o-mini
# AZURE_OPENAI_API_VERSION=2024-08-01-preview

# --- OpenAI Configuration ---
# OPENAI_API_KEY=sk-xxx
# OPENAI_MODEL=gpt-4o-mini

# =============================================================================
# MCP Server Configuration (SSE mode)
# =============================================================================
MCP_HOST=0.0.0.0
MCP_PORT=8888

# External hostname for image URLs (used when behind reverse proxy)
# MCP_EXTERNAL_HOST=stylist.polly.wang

# Enable HTTPS for image URLs (when behind SSL-terminating proxy)
# MCP_USE_SSL=true

# API Key for authentication (leave empty to disable)
# Generate with: python -c "import secrets; print('sty-' + secrets.token_urlsafe(24))"
# MCP_API_KEY=your-api-key
